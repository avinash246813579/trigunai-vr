# Large media — NOT committed to git. The optimized video and the HLS stream
# ship as GitHub Release assets and are hosted same-origin alongside the
# deployed page. See HANDOFF.md.
*.mp4
*.mov
*.m4v
*.webm

# HLS adaptive-streaming output (manifests + .ts segments) — shipped as a
# zipped Release asset, placed next to index.html at deploy time.
hls/
*.ts
*.m3u8
*.zip

# Transcode / build logs
transcode.log
*.log

# Local editor / tooling config
.claude/

# OS cruft
Thumbs.db
.DS_Store
desktop.ini
